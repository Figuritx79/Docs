# My Documentation


In git when you use a ssh key you need to configure git to sign the commit if you want.

```bash
git config --global gpg.format ssh

git config --global user.signingkey /PATH/TO/.SSH/KEY.PUB
```
Move to other file related with git

